name: Build

on:
  push:
    branches:
      - master
  pull_request:

# Every `run:` step executes inside `nix develop`, so devShell tools come
# straight from the flake with no per-step prefix.
defaults:
  run:
    shell: nix develop --command bash -e {0}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Install Nix
        uses: NixOS/nix-installer-action@6b8548fe06acfb0155a50ab5d561accb215764cc # main

      - name: Setup Nix cache (hestia)
        uses: Mic92/hestia/action@ff07bb902a9968ac0c3d0e51d90a606662a375d8 # main

      - name: Check flake
        run: nix flake check

      - name: Lint
        run: prek run --all-files --show-diff-on-failure

      - name: Build
        run: nix build .#hugin
